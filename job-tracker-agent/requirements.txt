# JOBFLOW — runtime and tooling requirements.
#
# The engine itself (jobtracker/*.py) imports ONLY the Python standard library:
# pip install is optional and only needed for the REST layer and the test suite.
#
#   pip install -r requirements.txt

fastapi>=0.110,<1.0
uvicorn>=0.27,<1.0
pydantic>=2.5,<3.0

# Test suite + parity harness
pytest>=8.0,<9.0
httpx>=0.27,<1.0
