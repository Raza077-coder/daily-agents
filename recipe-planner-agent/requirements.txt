# Core engine has ZERO third-party dependencies (standard library only).
# These are needed for the optional REST API surface and the test suite.

# --- REST API (optional) ---
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.5

# --- tests ---
pytest>=7.4
httpx>=0.27
